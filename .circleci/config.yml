version: 2
jobs:
  build:
    docker:
      - image: circleci/node:11.15.0

    working_directory: ~/repo

    steps:
      - checkout

      - deploy:
          name: Deploy to IPFS
          command: |
            npm run build
            npm run export
            npx ipfs-deploy -p pinata -d cloudflare build
