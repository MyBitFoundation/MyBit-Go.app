version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - deploy:
          name: Deploy to IPFS
          command: |
            yarn install --ignore-engines
            yarn run build
            yarn run export
