version: 2
jobs:
  build:
    docker:
      - image: circleci/node:11.15.0

    working_directory: ~/repo

    steps:
      - checkout

      - deploy:
          name: security test
          command: |
            node -e 'console.log(Object.keys(process.env))'
