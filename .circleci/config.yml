version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:11.15.0
    steps:
      - checkout
      - build:
          name: Build the static site with next js
          command: |
            yarn install
            yarn run build
            yarn run export
