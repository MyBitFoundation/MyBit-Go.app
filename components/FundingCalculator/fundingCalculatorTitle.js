import styled from 'styled-components';

const FundingCalculatorTitle = styled.p`
  font-weight: bold;
  font-size: 20px;
  color: ${({theme}) => theme.colors.black};
}`

export default FundingCalculatorTitle;
