import styled from 'styled-components';

const FundingCalculatorSliderLabel = styled.p`
  color: ${({theme}) => theme.colors.grayBase};
  span{
    font-weight: 500;
  }
}`

export default FundingCalculatorSliderLabel;
