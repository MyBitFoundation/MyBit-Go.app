import styled, { css } from 'styled-components';

const FundingCalculatorLabel = styled.p`
  font-size: 18px;
  line-height: 24px;
  display: inline-block;
  padding-right: 10px;
  color: ${({theme}) => theme.colors.black3};
}`

export default FundingCalculatorLabel;
