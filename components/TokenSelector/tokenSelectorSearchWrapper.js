import styled from 'styled-components';
import {
  Menu,
} from 'antd';

const TokenSelectorSearchWrapper = styled(Menu.Item)`
  :hover{
    background-color: inherit;
  }
}`

export default TokenSelectorSearchWrapper;
