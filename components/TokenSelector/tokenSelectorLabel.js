import styled from 'styled-components';

const TokenSelectorLabel = styled.span`
  font-size: 14px;
  color: ${({theme}) => theme.colors.black3};
  opacity: 0.8;
}`

export default TokenSelectorLabel;
