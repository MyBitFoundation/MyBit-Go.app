import styled from 'styled-components';

const StyledAssetDetailsInfoTitle = styled.b`
  font-size: 20px;
  color: rgba(0,0,0,.65);
  display: block;
  margin-top: 30px;
}`

export default StyledAssetDetailsInfoTitle;
