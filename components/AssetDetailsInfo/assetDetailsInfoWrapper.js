import styled from 'styled-components';

const AssetDetailsInfoWrapper = styled.div`
  box-shadow: 0 4px 12px 0 rgba(0,0,0,.1);
  padding: 20px;

  a:focus{
    text-decoration: none;
  }
}`

export default AssetDetailsInfoWrapper;
