import React from 'react';
import styled from 'styled-components';
import LocationIcon from '../../static/location-icon-blue.svg';

const StyledAssetDetailsInfoLocationIcon = styled(LocationIcon)`
  position: relative;
  top: 3px;
}`

export default StyledAssetDetailsInfoLocationIcon;
