import styled from 'styled-components';

const StyledstyledAssetCalculatorTitle = styled.p`
  font-weight: 700;
  margin-top: 40px;
  font-size: 20px;
}`

export default StyledstyledAssetCalculatorTitle;
