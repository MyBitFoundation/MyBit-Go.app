import React from 'react';
import styled from 'styled-components';
import LocationIcon from 'public/location-icon2.svg';

const AssetTemplateLocationIcon = styled(LocationIcon)`
  position: absolute;
  bottom: 20px;
  left: 21px;
  z-index: 1;
}`

export default AssetTemplateLocationIcon;
