import styled from 'styled-components';

const AssetPortfolioInvestmentContainer = styled.div`
  background-color: #ffffff;
  padding: 10px;
  color: rgba(0, 0, 0, 0.65);
}`

export default AssetPortfolioInvestmentContainer;
