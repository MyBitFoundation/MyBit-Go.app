import styled from 'styled-components';

const StyledAssetPortfolioManagedContainer = styled.div`
  background-color: #F9F9F9;
  padding: 10px;
  color: rgba(0, 0, 0, 0.65);
}`

export default StyledAssetPortfolioManagedContainer;
