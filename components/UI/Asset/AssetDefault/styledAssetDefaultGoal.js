import styled from 'styled-components';

const StyledAssetDefaultGoal = styled.p`
  text-align: right;
  font-size: 14px;
  overflow: hidden;
  white-space: nowrap;
}`

export default StyledAssetDefaultGoal;
