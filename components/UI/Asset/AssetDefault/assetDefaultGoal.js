import styled from 'styled-components';

const AssetDefaultGoal = styled.p`
  text-align: right;
  font-size: 14px;
  overflow: hidden;
  white-space: nowrap;
  margin-bottom: 0px;
}`

export default AssetDefaultGoal;
