import styled from 'styled-components';

const StyledAssetState = styled.div`
  p {
    font-size: 14px;
    color: #383838;
    padding-top: 6px;
    display: inline-block;
  }
}`

export default StyledAssetState;
