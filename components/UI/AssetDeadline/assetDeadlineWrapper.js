import styled from 'styled-components';

const AssetDeadlineWrapper = styled.div`
  p {
    font-size: 14px;
    padding-top: 6px;
    display: inline-block;
  }
}`

export default AssetDeadlineWrapper;
