import styled from 'styled-components';

const ValueDisplayIcon = styled.span`
  margin-right: 5px;
  position: relative;
  top: 4px;

  svg{
    height: 22px;
    width: 22px;
  }
}`

export default ValueDisplayIcon;
