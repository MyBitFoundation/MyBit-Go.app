import styled from 'styled-components';

const FundingDetailsTitle = styled.p`
  font-size: 14px;
  font-weight: 500;
  color: ${({theme}) => theme.colors.grayBase};
  margin: 0;
}`

export default FundingDetailsTitle;
