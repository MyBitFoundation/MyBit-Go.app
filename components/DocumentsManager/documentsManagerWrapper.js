import styled from 'styled-components';

const DocumentsManagerWrapper = styled.div`
  min-height: 400px;
  border-radius: 4px;
  box-shadow: 0 4px 12px 0 rgba(0,0,0,0.1);
}`

export default DocumentsManagerWrapper;
