import styled from 'styled-components';

const AssetFundingConfirmingMessage = styled.p`
  font-size: 16px;
  line-height: 24px;
  margin-top: 30px;
  color: ${({theme}) => theme.colors.grayBase};
}`

export default AssetFundingConfirmingMessage;
