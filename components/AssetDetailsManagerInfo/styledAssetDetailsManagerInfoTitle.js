import styled from 'styled-components';

const StyledDetailsManagerInfoTitle = styled.p`
  font-size: 20px;
  color: rgba(0,0,0,.65);
  display: block;
  font-weight: bolder;
  margin-bottom: 0;
  display: inline-block;
}`

export default StyledDetailsManagerInfoTitle;
