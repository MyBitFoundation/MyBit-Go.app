import styled from 'styled-components';

const AssetDetailsManagerInfoDocuments = styled.p`
  margin-top: 10px;
  font-weight: 500;
  margin-bottom: 0;
  color: ${({theme}) => theme.colors.black2};
}`

export default AssetDetailsManagerInfoDocuments;
