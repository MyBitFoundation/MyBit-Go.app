os: linux
language: node_js
cache:
  yarn: true
install: true
banches:
  only:
    - master
script:
  - yarn install
  - yarn run build
  - yarn run export
  - ./node_modules/.bin/ipfs-deploy --no-open -p pinata -d cloudflare out
