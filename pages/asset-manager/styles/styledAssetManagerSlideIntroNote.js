import styled from 'styled-components';

const StyledAssetManagerSlideIntroNote = styled.p`
  line-height: normal;
  font-weight: bold;
  font-size: 14px;
  color: rgba(0,0,0,0.65);
  padding: 25px 20px 0px 20px;

  @media(max-width: 600px) {
    padding: 22px 15px 0px 15px;
    text-align: justify;
  }
}`

export default StyledAssetManagerSlideIntroNote;
