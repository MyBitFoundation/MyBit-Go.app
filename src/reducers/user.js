const initialState = {
  userName: '0x123f681646d4a755815f9cb19e1acc8565a0c2ac',
  token: '',
  ethBalance: 20,
  myBitBalance: 215,
};

const user = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default user;
